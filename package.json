{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "yarn lerna run dev --parallel",
    "publish:packages": "yarn lerna run build && yarn lerna publish"
  },
  "devDependencies": {
    "@emotion/core": "^10.0.16",
    "@emotion/styled": "10.0.0",
    "@rebass/preset": "^4.0.2",
    "edit-json-file": "^1.3.1",
    "emotion-theming": "^10.0.14",
    "lerna": "^3.5.1",
    "rebass": "^4.0.2",
    "shelljs": "^0.8.3"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {}
}
